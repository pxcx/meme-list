import React from 'react';

import { Typography } from '@material-ui/core';

const AddSoundForm: React.FC = () => {
  return <React.Fragment>
    <Typography variant="h4" style={{textAlign: 'center', margin: '2rem 0'}}>
      Lamentamos!
    </Typography>
    <Typography style={{textAlign: 'center'}}>
      Esta página ainda não está pronta
    </Typography>
  </React.Fragment>;
}

export default AddSoundForm;